#include "holberton.h"

/**
 * print_triangle - print triagle
 * @size - size of triangle
 */
void print_triangle(int size)
{
	int i, j, t;	

	if (size <= 0)
		_putchar('\n');
	else
	{
		for (i = 0, i <= size, i ++)
		{
			_putchar(' ');
		}
		for (j = 0
	}
}
